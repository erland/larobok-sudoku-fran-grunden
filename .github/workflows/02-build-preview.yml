name: Build Preview

on:
  workflow_dispatch:

jobs:
  preview:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Python dependency
        run: python3 -m pip install --disable-pip-version-check pyyaml

      - name: Install pinned Pandoc
        uses: pandoc/actions/setup@v1
        with:
          version: 3.1.11.1

      - name: Install XeLaTeX, fonts and SVG conversion
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            texlive-xetex texlive-latex-extra texlive-fonts-recommended \
            fonts-texgyre lmodern librsvg2-bin

      - name: Build EPUB and PDF
        run: python3 scripts/build_publish.py --output-dir "${RUNNER_TEMP}/book-dist"

      - name: Upload preview artifact
        uses: actions/upload-artifact@v4
        with:
          name: sudoku-fran-grunden-preview
          path: |
            ${{ runner.temp }}/book-dist/*.epub
            ${{ runner.temp }}/book-dist/*.pdf
          if-no-files-found: error
